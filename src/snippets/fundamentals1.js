const js = `// declare immutable variables with "const"
const name = 'Johnny';
const nin = '[redacted]';
const PLAYER_ONE_UP_KEY = 'w';
// you wouldn't change these while your code executes

// declare mutable variables with "let"
let score = 0;
// you expect to change these while your code executes!
score = score + 1;

// never use "var" in modern Javascript!
`;

export default js;
