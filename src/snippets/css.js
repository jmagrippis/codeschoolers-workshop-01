const css = `div { /* styles every div */
  font-size: 32px;
}

.paddle { /* styles all elements with the "paddle" class */
  position: absolute;
  width: 10px;
  color: white;
}

#paddle-one { /* styles the one element that has this id */
  left: 10px;
}

#paddle-two { /* styles the one element that has this id */
  right: 10px;
}
`;

export default css;
